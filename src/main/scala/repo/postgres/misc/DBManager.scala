package repo.postgres.misc


object DBManager  {
//  import domain.User
//
//  val ctx = new PostgresZioJdbcContext(Literal)
//  import ctx._
//
//  def insert(user: User): ZIO[Any, Throwable, Unit] =
//    ZIO.fromFuture { implicit ec =>
//      ctx.run(query[User].insertValue(user)).unit
//    }


  // TODO: Need to pass this config info in after tested
//  val dataSource: ZLayer[Any, Throwable, DataSource] = Quill.DataSource.fromPrefix("testDbConfig")




}
